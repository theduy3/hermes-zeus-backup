# Absorbed skill packages

- `kanban-orchestrator` — decomposition and routing playbook for orchestrator profiles.
- `kanban-worker` — worker pitfalls, lifecycle edge cases, and examples.

The umbrella keeps orchestration and execution roles together while preserving the original packages in the archive.