# Goals summary

Strategic goals (the "why" + milestones). Actionable dated tasks live in `/vault/Tasks/` (authority). This summary shows current supported goals only.

## Current goals
- None recorded yet. Awaiting user-supplied goals (forward-only via `tracker/life_log.py --kind goal`).

## Notes
- Life OS stores strategic context; vault Tasks remain the task authority.
- Corrections preserve history (superseded events stay in the ledger).
