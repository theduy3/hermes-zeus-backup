# Private Life Tracker

Local URL: http://127.0.0.1:8788. Run `python3 app.py`. Browser cookie endpoint exists at `/api/login`; agent bearer endpoints are separate. Secrets are generated only in `~/.config/life-tracker/secrets.env` mode 600 and never printed. SQLite is operational state: Markdown at `/home/hermes/.hermes/projects/life-os/life-knowledge-base` remains knowledge authority. Use HTTPS reverse proxy and set secure cookies before remote exposure.
